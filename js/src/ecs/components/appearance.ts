import { IComponent } from './component';

export default class AppearanceComponent implements IComponent {
    public name: string = 'appearance';
    public color: string = 'rgba(255, 255, 255, 1.0)';
}
